name: idle_detector_wrapper
description: A comprehensive Flutter package for idle detection with full web scroll support, mouse movement, keyboard input, and touch gesture detection.
version: 1.2.0
repository: https://github.com/robrob-creator/idle_detector_wrapper

environment:
  sdk: ^3.0.3

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.0.0
  test: ^1.21.0
